@model SendMessageViewModel

<form asp-controller="Cliente" asp-action="EnviarMensaje" method="post" class="d-flex gap-2 align-items-start">
    <input type="hidden" asp-for="PropertyId" />
    <div class="flex-grow-1">
        <div asp-validation-summary="ModelOnly" class="text-danger small mb-1"></div>
        <textarea asp-for="Texto" class="form-control" rows="2" placeholder="Escribe tu mensaje..."></textarea>
        <span asp-validation-for="Texto" class="text-danger small"></span>
    </div>
    <button type="submit" class="btn btn-primary">Enviar mensaje</button>
</form>
